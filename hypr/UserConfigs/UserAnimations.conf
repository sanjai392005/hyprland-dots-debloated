# # ----------------- default ------------------------
# # animations {
  # # enabled = yes
# #
  # # bezier = wind, 0.05, 0.9, 0.1, 1.05
  # # bezier = winIn, 0.1, 1.1, 0.1, 1.1
  # # bezier = winOut, 0.3, -0.3, 0, 1
  # # bezier = liner, 1, 1, 1, 1
  # # bezier = overshot, 0.05, 0.9, 0.1, 1.05
  # # bezier = smoothOut, 0.5, 0, 0.99, 0.99
  # # bezier = smoothIn, 0.5, -0.5, 0.68, 1.5
  # #
  # # animation = windows, 1, 3, wind, slide
  # # animation = windowsIn, 1, 2, winIn, slide
  # # animation = windowsOut, 1, 2, smoothOut, slide
  # # animation = windowsMove, 1, 4, wind, slide
  # # animation = border, 1, 1, liner
  # # animation = borderangle, 1, 180, liner, loop #used by rainbow borders and rotating colors
  # # animation = fade, 1, 3, smoothOut
  # # animation = workspaces, 1, 3, overshot
  # #
  # # # animations for -git or version >0.42.0
  # # animation = workspacesIn, 1, 3, winIn, slide
  # # animation = workspacesOut, 1, 3, winOut, slide
# # }


# ----------------- first try ----------------------
# animations {
  # enabled = yes
# 
  # # Stronger, snappier curves
  # bezier = wind, 0.2, 1.0, 0.3, 1.0
  # bezier = winIn, 0.3, 1.2, 0.3, 1.0
  # bezier = winOut, 0.4, -0.2, 0.2, 1.0
  # bezier = liner, 1, 1, 1, 1
  # bezier = overshot, 0.2, 1.0, 0.3, 1.1
  # bezier = smoothOut, 0.4, 0.0, 0.2, 1.0
  # bezier = smoothIn, 0.4, 0.0, 0.2, 1.0
# 
	# animation = windows, 1, 2, wind, slide
	# animation = windowsIn, 1, 2, winIn, slide
	# animation = windowsOut, 1, 2, winOut, slide
	# animation = windowsMove, 1, 2, wind, slide
# 
	# animation = border, 1, 1, liner
	# animation = borderangle, 1, 180, liner, loop
	# animation = fade, 1, 2, smoothOut
# 
	# animation = workspaces, 1, 2, overshot
	# animation = workspacesIn, 1, 2, winIn, slide
	# animation = workspacesOut, 1, 2, winOut, slide
# 
  # 
# }

#----------------wild-turns------------------
# animations {
  # enabled = yes
# 
  # # Aggressive cubic beziers
  # bezier = snap, 0.1, 1.5, 0.2, 1.0
  # bezier = launch, 0.3, 1.7, 0.2, 1.0
  # bezier = recoil, 0.6, -0.4, 0.3, 1.2
  # bezier = elastic, 0.2, 1.4, 0.3, 1.3
  # bezier = smooth, 0.4, 0.0, 0.2, 1.0
  # bezier = linear, 1, 1, 1, 1
# 
  # animation = windows, 1, 3, snap, popin 80%
  # animation = windowsIn, 1, 3, launch, popin 90%
  # animation = windowsOut, 1, 3, recoil, popin 80%
  # animation = windowsMove, 1, 3, elastic
# 
  # animation = border, 1, 2, smooth
  # animation = borderangle, 1, 270, linear, loop
# 
  # animation = fade, 1, 2, smooth
  # 
  # animation = workspaces, 1, 4, elastic, slide
  # animation = workspacesIn, 1, 3, launch, slide
  # animation = workspacesOut, 1, 3, recoil, slide
# }


#--------------------- wild-turns-2-----------------
animations {
  enabled = yes

  # curves
  bezier = snap, 0.1, 1.5, 0.2, 1.0
  bezier = launch, 0.3, 1.7, 0.2, 1.0
  bezier = recoil, 0.6, -0.4, 0.3, 1.2
  bezier = elastic, 0.2, 1.4, 0.3, 1.3
  bezier = smooth, 0.4, 0.0, 0.2, 1.0
  bezier = linear, 1, 1, 1, 1
  bezier = openCurve, 0.2, 1.2, 0.3, 1.0
  bezier = closeCurve, 0.4, 0.0, 0.2, 1.0
  bezier = moveCurve, 0.2, 1.1, 0.3, 1.0

# Windows
	animation = windows, 1, 2, openCurve, popin 88%
	animation = windowsIn, 1, 2, openCurve, popin 88%
	animation = windowsOut, 1, 2, closeCurve
	animation = windowsMove, 1, 2, moveCurve

  # Borders
  animation = border, 1, 1, smooth
  animation = borderangle, 1, 180, linear, loop

  # Fade — snappier
  animation = fade, 1, 1, smooth

  # Workspaces
  animation = workspaces, 1, 3, elastic, slide
  animation = workspacesIn, 1, 2, launch, slide
  animation = workspacesOut, 1, 2, recoil, slide
}
